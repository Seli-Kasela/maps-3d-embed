version: '3.8'

services:
  maps-3d-embed:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - GOOGLE_MAPS_API_KEY=${GOOGLE_MAPS_API_KEY}
      - ENVIRONMENT=development
    volumes:
      - ./static:/root/static
    restart: unless-stopped
